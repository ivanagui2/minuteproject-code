#exposeJOOQApplicationSpecific()
#exposeJOOQEntitySpecific()
#addedAreas()
/**
 * This class is generated by minuteproject 4 jOOQ
 */
package $packageName;

import $recordClassImport;
import $modelClassImport;
import $keysClassImport;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

import $modelFactoryClassImport;
import static ${entityClassImport}.$classNameUpperCase;

import org.jooq.Record;
import org.jooq.Result;
import org.junit.Test;

$!updatedAreaImport

$!updatedAreaClassAnnotation
@javax.annotation.Generated(value    = {"http://www.jooq.org", "$jooqVersion"},
                            comments = "This class is generated by minuteproject 4 jOOQ")
public class $className {

	@Test
	public void test$entityClass () {
		Connection conn = null;
		String userName = "$datasource.username";
		String password = "$datasource.password";
		String url = "$datasource.url";

		try {
			Class.forName("$datasource.driverClassName").newInstance();
			conn = DriverManager.getConnection(url, userName, password);
			$modelFactoryClass create = new ${modelFactoryClass}(conn);

#exposeModelUpdatedAreas("jooq-unittest-test$entityClass")
$!updatedAreaBegin
#if($isUpdated)
$!updatedAreaSnippet
#else
			// write your own tests, just set DISABLE to ENABLE in the comment above
			// future generation will not erase your code ;)
			Result<Record> result = create.select().from($classNameUpperCase).limit(1).fetch();
			for (Record r : result) {
#foreach ($column in ${table.columns})
#putJOOQColumnParam()
#if($isEnum)
#exposeJOOQEnum()
#end
				$jooqColumnFullType $columnVar = r.getValue(${classNameUpperCase}.${columnVariableUpperCase});
#end
				System.out.println(
#foreach ($column in ${table.columns})
#putJOOQColumnParam()
#if($isEnum)
#exposeJOOQEnum()
#end
					"$columnVar : "+ $columnVar #if ($velocityCount!=$table.columnCount)+
#end
#end

					);
#end
$!updatedAreaEnd
			}
			
		} catch (Exception e) {
			e.printStackTrace();
		} finally {
			if (conn != null) {
				try {
					conn.close();
				} catch (SQLException e) {
					// TODO Auto-generated catch block
					e.printStackTrace();
				}
			}
		}
	}
$!updatedAreaImplementation	

}
