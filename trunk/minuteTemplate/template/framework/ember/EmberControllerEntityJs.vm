##import BaseRoute from '$pathToEmberBaseRoute';//'../../base'
import Ember from 'ember';

import BaseRoute from '../base';

import _ from 'lodash';
##import {toDisplay,setDisplayCount, convertComposition} from '../../utils/business-utils';

import config from '../../config/environment';
export default BaseRoute.extend({
	titleToken: ['field.${query.name}'],
	${model.name}Service : Ember.inject.service("${model.name}"),
	model(params) {
        return this.get('${model.name}Service').${function.name}(params);
    },
});
