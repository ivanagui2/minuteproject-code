#exposeVariableEntitySpecific()
#exposeRESTURL()
#set ($cpt = 0)
<!DOCTYPE html PUBLIC "-//W3C//  DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
 <html>
   <head>
     <meta content="text/html; charset=iso-8859-1" http-equiv="Content-Type">
     <title>Address book</title>
     <script src="http://ajax.microsoft.com/ajax/jquery/jquery-1.4.4.min.js" type="text/javascript"></script>
     <script type="application/javascript">
     $(function() {   
         $('#search${domainClassName} a').click(function() {
             jQuery.ajax({
                 type: "GET",
                 dataType: "xml",
				 url : "$restXmlServerUrl/"+$('#${primaryKeyVariable}').val(),
                 success: function(xml) {
                    $(xml).find('${domainVariableName}').each(function(){
                         var name_text = $(this).find('name').text()
#foreach($column in ${table.primaryKeyColumns})
#putColumnParams()
						 var ${columnVar}_text = $(this).find('${columnVar}').text()							 
#end
#foreach ($column in ${table.attributes})
#putColumnParams()
						 var ${columnVar}_text = $(this).find('${columnVar}').text()
#end                          
                         $('<li></li>')
                             .html(
#foreach($column in ${table.primaryKeyColumns})
#putColumnParams()
								 #if($cpt>0)+" - "+#else #end${columnVar}_text
#set ($cpt=$cpt+1)								 
#end
#foreach ($column in ${table.attributes})
#putColumnParams()
								 #if($cpt>0)+" - "+#else #end${columnVar}_text
#set ($cpt=$cpt+1)	
#end                             
                             )
                             .appendTo('#search${domainClassName} ol')
                     })
                 }
             })
         })
 
     })
     </script>
   </head>
   <body>
     <p>
       <div id='search${domainClassName}'>
         <a href="#">Click here to add ${domainVariableName} description with $primaryKeyVariable =</a>&nbsp;<input id="$primaryKeyVariable" size="20" type="text"/>
         <ol></ol>
       </div>
     </p>
   </body>
 </html>